# Example env file for `docker-compose.metabase.external.yml`
# Copy to `.env.metabase` and fill with your real DB values before running.

# Hostname or IP of your Postgres server. If Postgres runs on the same host as Docker Desktop, you can use `host.docker.internal`.
MB_DB_HOST=host.docker.internal
# Postgres port (default 5432)
MB_DB_PORT=5432
# Database name Metabase will use to store its metadata (can be existing DB)
MB_DB_DBNAME=metabase
# DB user/password with access to the above DB
MB_DB_USER=metabase_user
MB_DB_PASS=metabase_password
