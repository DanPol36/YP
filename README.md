# YP — документы и клиенты

Простое, удобное и понятное приложение на Flask для работы с документами и базой клиентов. Этот README — краткий обзор проекта без технических фрагментов, чтобы быстро сориентироваться.

**Что это**
- **Цель**: хранение документов и управление клиентской базой, включая работу с устаревшей (legacy) таблицей.
- **Стек**: Flask, SQLAlchemy, Jinja2; есть вспомогательные скрипты для профилирования и обслуживания.

**Короткий обзор**
- 🧰 **Код**: основные модули в папке `app`, шаблоны в `app/templates`, модели в `app/models`.
- 🧾 **Legacy**: для старой таблицы `practic2` предусмотрена отдельная модель — её колонки имеют русские имена, с ними следует быть осторожными при изменениях.

**Как начать (в общих чертах)**
- 🧾 **Установите зависимости**: проект использует стандартное Python-окружение и зависимости перечислены в `requirements.txt`.
- **Настройка**: создайте локальный файл с параметрами окружения, чтобы приложение знало, куда подключаться (база данных, секреты и т.п.).
- **Первый запуск**: при первом старте приложение создаёт администратора по умолчанию — это удобно для тестирования.

**Docker и Metabase — кратко**
- 🐳 **Docker**: можно поднять базу и Metabase в контейнерах для быстрого тестирования и аналитики без сложной ручной настройки.
- 📊 **Metabase**: лёгкий веб-инструмент для построения отчётов и дешбордов поверх вашей базы данных.
- **Варианты**: запускать всё в контейнерах или запускать приложение локально и подключать Metabase отдельно к базе.
- **Совет**: перед удалением контейнеров делайте бэкап данных, если они важны.

**База данных и миграции**
- **Миграции**: проект настроен для использования миграций; при изменениях схемы учитывайте особенности legacy-таблицы с русскими именами колонок.

**Шаблоны и маршруты**
- **Шаблоны**: смотрите `app/templates`.
- **Маршруты**: основные области — авторизация, документы и клиенты (routes/blueprints внутри `app/routes`).

**Профилирование и утилиты**
- В проекте есть несколько скриптов и утилит для профилирования и генерации отчётов о производительности; они находятся в папке `scripts`.

---

## Подробная справка (актуально для текущей версии проекта)

Ниже — расширенная версия README, адаптированная под текущую структуру репозитория. Она содержит практическую информацию и команды, которые обычно нужны при локальной работе и разработке.

### Что внутри
- `run.py` — точка входа для запуска приложения в режиме разработчика и для инициализации (создаёт администратора при первом старте).
- `app/` — код приложения: фабрика `create_app()`, blueprints, маршруты, шаблоны.
- `app/models/` — модели SQLAlchemy для основных сущностей; есть отдельная legacy-модель для `practic2`.
- `app/routes/` — реализованные маршруты, в том числе `auth`, `documents`, `people`.
- `app/templates/` — Jinja2-шаблоны для страниц.
- `instance/database.py` — конфигурация для работы с legacy-таблицей и созданием движка при необходимости.
- `scripts/` — набор утилит для профилирования и вспомогательных задач.
- `sql/` — (по возможности) SQL-скрипты и view-выражения для аналитики.
- `assets/` — простые иллюстрации и иконки для README.

### Ключевые моменты
- Фабрика приложения: `create_app()` в `app/__init__.py`.
- Используется Flask + Flask-SQLAlchemy; для миграций подключён Flask-Migrate.
- Legacy-таблица `practic2` маппится отдельно из-за нестандартных (кириллических) имён колонок — часть работы с ней выполняется через сырые SQL-запросы.

---

## Требования
- Python 3.11+ (рекомендуется). Проект обычно работает и на более ранних 3.x, но тестируйте окружение.
- Зависимости перечислены в `requirements.txt`.

Рекомендуется использовать виртуальное окружение для разработки.

---

## Конфигурация
- Создайте локальный файл окружения `.env` в корне проекта и пропишите минимум следующих переменных:
	- `DATABASE_URL` — строка подключения к Postgres (локальный или тестовый сервер).
	- `SECRET_KEY` — секрет приложения.

- Файл `.env` не должен попадать в публичные репозитории: не храните реальные секреты в открытом виде.

---

## Запуск приложения (локально)

1. Подготовьте виртуальное окружение и установите зависимости (локально).
2. Настройте `.env` с правильной строкой подключения к базе и секретом.
3. Запустите `run.py` для разработки или используйте Flask CLI с указанием `FLASK_APP=run.py`.

При первом запуске `run.py` автоматически создаёт администратора (логин `admin`, пароль `12345`) — используйте это для доступа в локальной среде, но смените пароль в реальном использовании.

---

## База данных и миграции

- Проект использует Flask-Migrate для управления миграциями. Миграции хранятся в миграционной папке (если инициализация была выполнена).
- Особенность: таблица `practic2` — legacy-таблица с кириллическими именами колонок (например, `"ФИО"`, `"Номер_телефона"`). Для неё в коде используются защищённые/сырые SQL-запросы, чтобы избежать проблем с автоматическим маппингом.

Рекомендуется:
- Перед изменением схемы сделать бэкап данных.
- Если вы вносите изменения в legacy-таблицу — тщательно тестируйте запросы, так как названия колонок чувствительны.

---

## Шаблоны и маршруты

- Основные шаблоны находятся в `app/templates` (`layout.html`, `login.html`, `clients.html`, `person_form.html`, `person_view.html`, `document_form.html`, `documents.html`).
- Маршруты организованы в blueprints: `auth`, `documents`, `people`. По умолчанию клиенты доступны по `/clients`.

---

## Профилирование и утилиты

- В папке `scripts/` есть утилиты для профилирования и генерации flamegraph-отчётов (py-spy, pyinstrument и cProfile + gprof2dot).
- Также есть `tools/dot_viewer.html` — простой DOT-viewer для локального просмотра результатов в браузере.

Если планируете профилировать, заранее установите необходимые инструменты и убедитесь, что путь к `dot` (Graphviz) доступен, если хотите получать SVG-графики.

---

## Docker и Metabase (анализ данных)

- В репозитории есть конфигурации и примеры для запуска Metabase и Postgres в контейнерах (Docker Compose). Metabase удобно использовать для быстрой визуализации данных и создания дешбордов.
- Варианты использования:
	- поднять Postgres и Metabase вместе для локальной аналитики;
	- запускать приложение локально и подключать отдельный экземпляр Metabase к базе данных.

Практический совет: если Metabase запускается в контейнере, а база — на хосте, используйте адрес, позволяющий контейнеру достучаться до хоста.

---

## Утилиты и вспомогательные скрипты

- `scripts/remove_unneeded.py` — очистка временных директорий и лишних файлов.
- `scripts/profile_pyspy.ps1` — помощник для py-spy профилирования.
- `scripts/convert_prof.ps1` — конвертация профайла в DOT и попытка запустить Graphviz.
- `sql/` — содержит вспомогательные SQL-запросы/VIEW-скрипты для аналитики (если присутствуют).

---

## Важные замечания

- Legacy-таблица `practic2` использует русские имена колонок — будьте внимательны при миграциях и переименованиях.
- Не храните реальные пароли и секреты в публичных репозиториях.
- Для продакшена рекомендуется фиксировать версии зависимостей и применить практики безопасного хранения секретов.

---

## Куда смотреть в коде
- `app/__init__.py` — фабрика приложения и регистрация blueprints.
- `app/routes/` — реализация маршрутов и логики для документов и клиентов.
- `app/models/` — SQLAlchemy-модели и legacy-модель `practic`.
- `instance/database.py` — отдельная конфигурация/engine для legacy-модели.
- `scripts/` — инструменты и скрипты для профилирования и поддержки.

---